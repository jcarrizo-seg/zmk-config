# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  # Versiones normales
  - board: nice_nano_v2
    shield: matricialsplit_dongle
    artifact-name: matricialsplit_dongle
    
  # - board: nice_nano_v2  
  #   shield: matricialsplit_peripheral_left
  #   artifact-name: matricialsplit_left
    
  # - board: nice_nano_v2  
  #   shield: matricialsplit_peripheral_right
  #   artifact-name: matricialsplit_right
    
  # - board: nice_nano_v2  
  #   shield: settings_reset
  #   artifact-name: settings_reset
    
  # # Versión debug del dongle (con logging USB habilitado)
  # - board: nice_nano_v2
  #   shield: matricialsplit_dongle
  #   cmake-args: -DCONFIG_ZMK_USB_LOGGING=y -DCONFIG_LOG_MODE_IMMEDIATE=y -DCONFIG_LOG_BUFFER_SIZE=4096
  #   artifact-name: matricialsplit_dongle_debug
